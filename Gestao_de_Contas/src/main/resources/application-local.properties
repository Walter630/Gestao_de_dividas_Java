
spring.application.name=Gestao_de_Contas

# Banco
spring.datasource.url=jdbc:postgresql://postgres:5432/${DB_NAME}
spring.datasource.username=gestao_contas
spring.datasource.password=admin

# RabbitMQ
spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.rabbitmq.username=guest
spring.rabbitmq.password=guest

# JWT
security.token=${JWT_SECRET}

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.data.redis.password=sua_senha_redis_aqui
spring.data.redis.timeout=2000ms

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.threads.virtual.enabled=true

# Swagger ? le do .env, padrao false se nao encontrar
springdoc.swagger-ui.enabled=${SWAGGER_ENABLED:false}
springdoc.api-docs.enabled=${SWAGGER_ENABLED:false}
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=walterro465@gmail.com
spring.mail.password="gdcf tabh fvmk aqcp"
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
